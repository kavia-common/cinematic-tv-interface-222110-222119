<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use a built-in platform theme to avoid resource linking issues; Compose applies M3 dark at runtime -->
    <style name="Theme.App.Startup" parent="@android:style/Theme.DeviceDefault.NoActionBar" />
</resources>
